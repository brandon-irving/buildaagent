# BuildAAgent Runtime Configuration

# Server Configuration
PORT=3000
LOG_LEVEL=info

# AI Provider Configuration
AI_PROVIDER=anthropic
AI_KEY_REF=ANTHROPIC_API_KEY

# For Anthropic (Claude) - Add your API key
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# For OpenAI (ChatGPT) - Alternative option
# AI_PROVIDER=openai
# AI_KEY_REF=OPENAI_API_KEY
# OPENAI_API_KEY=your-openai-api-key-here

# File System Paths
WORKSPACE_PATH=./workspace
PERSONAS_PATH=./config/personas
SKILLS_PATH=./src/skills

# Database Configuration (Optional - uses SQLite by default)
# DATABASE_PATH=./workspace/database

# CORS Configuration (Optional - allows all origins by default in dev)
# CORS_ORIGINS=http://localhost:19000,http://localhost:19001

# Gmail OAuth Configuration (Required for email-manager skill)
# Create OAuth 2.0 credentials at https://console.cloud.google.com
# Enable the Gmail API and set authorized redirect URI
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
GOOGLE_REDIRECT_URI=https://auth.expo.io/@brandonirving/buildaagent-mobile

# Token Encryption Key (Required for Gmail integration)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_ENCRYPTION_KEY=your-64-char-hex-key-here